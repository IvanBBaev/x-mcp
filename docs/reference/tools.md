<!-- GENERATED FILE — DO NOT EDIT BY HAND. -->
<!-- Generated by scripts/docs-gen.mjs from the live tool registry. -->
<!-- `npm run docs:gen` rewrites this file; `npm run docs:check` fails CI on drift. -->

# Tool reference (generated)

**41 tools in 12 packages.** Every field on this page is read out of the registered
`ToolDef` values, so this is the surface the server actually exposes — not the surface it
was designed to have. The designed catalog, including tools that are not implemented yet,
is [`../03-tool-catalog.md`](../03-tool-catalog.md).

## Packages

| Package | Tools | Registered tools |
|---|--:|---|
| `auth` | 2 | `x_auth_status`, `x_rate_limit_status` |
| `posts` | 4 | `x_post_get`, `x_post_create`, `x_post_delete`, `x_post_hide_reply` |
| `users` | 1 | `x_user_get` |
| `search` | 2 | `x_search_recent`, `x_post_counts_recent` |
| `engagement` | 4 | `x_like_set`, `x_repost_set`, `x_bookmark_set`, `x_bookmarks_list` |
| `timelines` | 3 | `x_timeline_home`, `x_timeline_mentions`, `x_timeline_user` |
| `graph` | 6 | `x_follow_set`, `x_mute_set`, `x_block_set`, `x_followers_list`, `x_following_list`, `x_user_search` |
| `lists` | 10 | `x_list_create`, `x_list_update`, `x_list_delete`, `x_list_get`, `x_lists_owned`, `x_list_member_set`, `x_list_members`, `x_list_timeline`, `x_list_follow_set`, `x_list_pin_set` |
| `media` | 2 | `x_media_upload`, `x_media_status` |
| `dm` | 4 | `x_dm_events_list`, `x_dm_conversation_events_list`, `x_dm_participant_events_list`, `x_dm_send` |
| `archive` | 2 | `x_search_archive`, `x_post_counts_archive` |
| `usage` | 1 | `x_usage_get` |

## Policy presets

Callable = tools whose policy cell the preset grants. Denied tools stay registered and
annotated; they refuse the call (POL-7). DM cells are in no preset, not even `full`.

| Preset | Callable | Denied |
|---|--:|--:|
| `read-only` *(default)* | 21 | 20 |
| `engage` | 26 | 15 |
| `publish` | 32 | 9 |
| `manage` | 34 | 7 |
| `full` | 37 | 4 |
| `full` + `X_MCP_POLICY_ALLOW=read:dm,write:dm` | 41 | 0 |

## Reading the tables

- **Cell** is the `operation:domain` policy classification ([../04-security.md](../04-security.md) §3).
- **Availability** is the credential class the tool needs — `app+user` works with an
  app-only bearer token, `user-only` requires OAuth 2.0 user context.
- **Cost** is the pay-per-use bucket; the price table lives in
  [`../01-api-landscape.md`](../01-api-landscape.md) §3. `x_post_create` resolves per call —
  `w:post` normally, but a post whose text contains a URL costs 13× more.
- **MCP hints** are derived from the cell, never hand-set (MCP-4).
- A nested field (`poll.options`) is required only when its parent object is supplied.
- Every input schema rejects unknown fields; only the fields listed are accepted.

## `auth`

### `x_auth_status`

**Auth status** — Report the active auth mode, the authenticated user (in user mode), granted OAuth scopes, the credential backend, detected availability, and the resolved policy matrix.

| Package | Cell | Availability | Cost | Phase | MCP hints | OAuth scopes |
|---|---|---|---|---|---|---|
| `auth` | `read:account` | `app+user` | `owned` | 1 | read-only | `users.read` |

Takes no input fields.

### `x_rate_limit_status`

**Rate-limit status** — Dump the in-process rate-limit table — per bucket (endpoint-class × auth-context), each tracked window's limit, remaining, reset time, and whether it is currently exhausted.

| Package | Cell | Availability | Cost | Phase | MCP hints | OAuth scopes |
|---|---|---|---|---|---|---|
| `auth` | `read:account` | `app+user` | `local` | 1 | read-only | — |

Takes no input fields.

## `posts`

### `x_post_get`

**Get posts** — Batch-fetch one or more X (Twitter) posts by numeric id or status URL (1-100 per call).

| Package | Cell | Availability | Cost | Phase | MCP hints | OAuth scopes |
|---|---|---|---|---|---|---|
| `posts` | `read:content` | `app+user` | `r:post` | 1 | read-only | `tweet.read`, `users.read` |

| Field | Type | Required | Description |
|---|---|:--:|---|
| `ids` | string[] (1–100 items) | ✅ | Post references to fetch. Each is a numeric post id or a full status URL (e.g. https://x.com/user/status/123). 1-100 per call. |
| `raw` | boolean |  |  |

### `x_post_create`

**Create post** — Create a post — text, optional reply_to_id, quote_id, media_ids[], poll {options[], duration_minutes}, reply_settings.

| Package | Cell | Availability | Cost | Phase | MCP hints | OAuth scopes |
|---|---|---|---|---|---|---|
| `posts` | `write:content` | `user-only` | `w:post` | 2 | — | `tweet.read`, `tweet.write`, `users.read` |

| Field | Type | Required | Description |
|---|---|:--:|---|
| `text` | string (non-empty) | ✅ | The post text, sent byte-identical (no normalization or trimming). Note: a URL in the text raises the per-post cost to $0.20. |
| `reply_to_id` | string |  | Post this replies to: a numeric post id or a full status URL. |
| `quote_id` | string |  | Post this quotes: a numeric post id or a full status URL. |
| `media_ids` | string[] (1–4 items) |  | Previously uploaded media ids, max 4. X allows up to 4 images OR a single video/GIF; media types are not knowable from bare ids, so the platform enforces the one-video rule. Mutually exclusive with poll. |
| `poll` | object |  | Attach a poll. Mutually exclusive with media_ids. |
| `poll.options` | string[] (2–4 items) | ✅ | 2-4 poll choices, each at most 25 characters. |
| `poll.duration_minutes` | integer (5–10080) | ✅ | Poll duration in minutes: 5 to 10080 (7 days). |
| `reply_settings` | `following` \| `mentionedUsers` \| `subscribers` |  | Who may reply. Omit to allow everyone. |

### `x_post_delete`

**Delete post** — Delete own post by id.

| Package | Cell | Availability | Cost | Phase | MCP hints | OAuth scopes |
|---|---|---|---|---|---|---|
| `posts` | `destructive:content` | `user-only` | `w:action` | 2 | destructive, non-idempotent | `tweet.read`, `tweet.write`, `users.read` |

| Field | Type | Required | Description |
|---|---|:--:|---|
| `id` | string (non-empty) | ✅ | The post to delete: a numeric post id or a full status URL. Own posts only. |

### `x_post_hide_reply`

**Hide / unhide a reply** — Hide or unhide a reply to one of your own posts.

| Package | Cell | Availability | Cost | Phase | MCP hints | OAuth scopes |
|---|---|---|---|---|---|---|
| `posts` | `write:moderation` | `user-only` | `w:action` | 3 | — | `tweet.read`, `tweet.moderate.write`, `users.read` |

| Field | Type | Required | Description |
|---|---|:--:|---|
| `reply_id` | string (non-empty) | ✅ | The reply to hide or unhide: the REPLY's own numeric post id or status URL, inside a conversation started by the authenticated account. |
| `action` | `hide` \| `unhide` | ✅ | Whether to hide or unhide the reply. |

## `users`

### `x_user_get`

**Get users** — Batch fetch of X (Twitter) user profiles by numeric id, @handle, bare handle, or the sentinel `me` (the authenticated user).

| Package | Cell | Availability | Cost | Phase | MCP hints | OAuth scopes |
|---|---|---|---|---|---|---|
| `users` | `read:user` | `app+user` | `r:user` | 1 | read-only | `users.read` |

| Field | Type | Required | Description |
|---|---|:--:|---|
| `users` | string[] (1–100 items) | ✅ | user id, @handle, handle, or 'me' |
| `raw` | boolean |  |  |

## `search`

### `x_search_recent`

**Search recent posts** — Search X (Twitter) posts from the last 7 days using the full v2 query syntax (from:, to:, conversation_id:, boolean operators).

| Package | Cell | Availability | Cost | Phase | MCP hints | OAuth scopes |
|---|---|---|---|---|---|---|
| `search` | `read:content` | `app+user` | `r:post` | 1 | read-only | `tweet.read`, `users.read` |

| Field | Type | Required | Description |
|---|---|:--:|---|
| `query` | string (non-empty) | ✅ | X (Twitter) v2 search query, e.g. "from:xdevelopers -is:retweet". |
| `max_results` | integer |  | Results per page (10-100); out-of-range values are clamped into the window. |
| `page_token` | string |  | Opaque pagination cursor returned as next_token by a previous call. |
| `start_time` | string |  | Oldest post timestamp to include (ISO-8601 UTC). |
| `end_time` | string |  | Newest post timestamp to include (ISO-8601 UTC). |
| `sort_order` | `recency` \| `relevancy` |  | Result ordering; defaults to recency. |
| `raw` | boolean |  | Return the exact API JSON (capped at 25 items) instead of the compact page. |

### `x_post_counts_recent`

**Count recent posts** — Return a volume histogram (post counts per time bucket) for an X (Twitter) v2 query over the last 7 days, at minute/hour/day granularity.

| Package | Cell | Availability | Cost | Phase | MCP hints | OAuth scopes |
|---|---|---|---|---|---|---|
| `search` | `read:content` | `app+user` | `r:post` | 1 | read-only | `tweet.read` |

| Field | Type | Required | Description |
|---|---|:--:|---|
| `query` | string (non-empty) | ✅ | X (Twitter) v2 search query to count. |
| `granularity` | `minute` \| `hour` \| `day` |  | Histogram bucket size; defaults to hour. |
| `start_time` | string |  | Oldest bucket timestamp (ISO-8601 UTC). |
| `end_time` | string |  | Newest bucket timestamp (ISO-8601 UTC). |
| `page_token` | string |  | Opaque pagination cursor returned as next_token by a previous call. |
| `raw` | boolean |  | Return the exact API JSON instead of the compact histogram. |

## `engagement`

### `x_like_set`

**Like / unlike a post** — Like or unlike a post as the authenticated user.

| Package | Cell | Availability | Cost | Phase | MCP hints | OAuth scopes |
|---|---|---|---|---|---|---|
| `engagement` | `write:engagement` | `user-only` | `w:action` | 2 | idempotent | `tweet.read`, `users.read`, `like.write` |

| Field | Type | Required | Description |
|---|---|:--:|---|
| `post` | string (non-empty) | ✅ | Target post: a numeric post id or a full status URL (e.g. https://x.com/u/status/1). |
| `action` | `like` \| `unlike` | ✅ | Whether to add or remove the like. |

### `x_repost_set`

**Repost / un-repost a post** — Repost (retweet) a post as the authenticated user, or undo that repost.

| Package | Cell | Availability | Cost | Phase | MCP hints | OAuth scopes |
|---|---|---|---|---|---|---|
| `engagement` | `write:engagement` | `user-only` | `w:action` | 2 | idempotent | `tweet.read`, `users.read`, `tweet.write` |

| Field | Type | Required | Description |
|---|---|:--:|---|
| `post` | string (non-empty) | ✅ | Target post: a numeric post id or a full status URL (e.g. https://x.com/u/status/1). |
| `action` | `repost` \| `unrepost` | ✅ | Whether to repost or undo the repost. |

### `x_bookmark_set`

**Bookmark / unbookmark a post** — Add a post to the authenticated user's bookmarks or remove it.

| Package | Cell | Availability | Cost | Phase | MCP hints | OAuth scopes |
|---|---|---|---|---|---|---|
| `engagement` | `write:engagement` | `user-only` | `w:action` | 2 | idempotent | `tweet.read`, `users.read`, `bookmark.write` |

| Field | Type | Required | Description |
|---|---|:--:|---|
| `post` | string (non-empty) | ✅ | Target post: a numeric post id or a full status URL (e.g. https://x.com/u/status/1). |
| `action` | `add` \| `remove` | ✅ | Whether to add or remove the bookmark. |

### `x_bookmarks_list`

**List own bookmarks** — The authenticated user's own bookmarks, newest first — the read half of `x_bookmark_set`.

| Package | Cell | Availability | Cost | Phase | MCP hints | OAuth scopes |
|---|---|---|---|---|---|---|
| `engagement` | `read:content` | `user-only` | `owned` | 3 | read-only | `tweet.read`, `users.read`, `bookmark.read` |

| Field | Type | Required | Description |
|---|---|:--:|---|
| `max_results` | integer |  | Results per page (1-100); out-of-range values are clamped into the window. |
| `page_token` | string |  | Opaque pagination cursor returned as next_token by a previous call. |
| `raw` | boolean |  | Return the exact API JSON (capped at 25 items) instead of the compact page. |

## `timelines`

### `x_timeline_home`

**Read home timeline** — Read the authenticated X (Twitter) user's home timeline in reverse-chronological order (the accounts they follow, newest first).

| Package | Cell | Availability | Cost | Phase | MCP hints | OAuth scopes |
|---|---|---|---|---|---|---|
| `timelines` | `read:content` | `user-only` | `r:post` | 2 | read-only | `tweet.read`, `users.read` |

| Field | Type | Required | Description |
|---|---|:--:|---|
| `max_results` | integer |  | Results per page (5-100); out-of-range values are clamped into the window. |
| `page_token` | string |  | Opaque pagination cursor returned as next_token by a previous call. |
| `start_time` | string |  | Oldest post timestamp to include (ISO-8601 UTC). |
| `end_time` | string |  | Newest post timestamp to include (ISO-8601 UTC; values inside the last 10 seconds are adjusted). |
| `raw` | boolean |  | Return the exact API JSON (capped at 25 items) instead of the compact page. |

### `x_timeline_mentions`

**Read mentions timeline** — Read posts mentioning an X (Twitter) user (defaults to the authenticated user).

| Package | Cell | Availability | Cost | Phase | MCP hints | OAuth scopes |
|---|---|---|---|---|---|---|
| `timelines` | `read:content` | `app+user` | `owned` | 2 | read-only | `tweet.read`, `users.read` |

| Field | Type | Required | Description |
|---|---|:--:|---|
| `user` | string (non-empty) |  | User whose mentions to read: numeric id, handle, @handle, profile URL, or "me" (default). |
| `max_results` | integer |  | Results per page (5-100); out-of-range values are clamped into the window. |
| `page_token` | string |  | Opaque pagination cursor returned as next_token by a previous call. |
| `start_time` | string |  | Oldest post timestamp to include (ISO-8601 UTC). |
| `end_time` | string |  | Newest post timestamp to include (ISO-8601 UTC; values inside the last 10 seconds are adjusted). |
| `raw` | boolean |  | Return the exact API JSON (capped at 25 items) instead of the compact page. |

### `x_timeline_user`

**Read a user timeline** — Read an X (Twitter) user's own posts, newest first, optionally excluding replies and/or reposts, within optional time bounds.

| Package | Cell | Availability | Cost | Phase | MCP hints | OAuth scopes |
|---|---|---|---|---|---|---|
| `timelines` | `read:content` | `app+user` | `r:post` | 2 | read-only | `tweet.read`, `users.read` |

| Field | Type | Required | Description |
|---|---|:--:|---|
| `user` | string (non-empty) | ✅ | User to read: numeric id, handle, @handle, profile URL, or "me". |
| `exclude_replies` | boolean |  | Omit the user's replies. |
| `exclude_reposts` | boolean |  | Omit the user's reposts (retweets). |
| `max_results` | integer |  | Results per page (5-100); out-of-range values are clamped into the window. |
| `page_token` | string |  | Opaque pagination cursor returned as next_token by a previous call. |
| `start_time` | string |  | Oldest post timestamp to include (ISO-8601 UTC). |
| `end_time` | string |  | Newest post timestamp to include (ISO-8601 UTC; values inside the last 10 seconds are adjusted). |
| `raw` | boolean |  | Return the exact API JSON (capped at 25 items) instead of the compact page. |

## `graph`

### `x_follow_set`

**Follow / unfollow a user** — Follow or unfollow a user as the authenticated user.

| Package | Cell | Availability | Cost | Phase | MCP hints | OAuth scopes |
|---|---|---|---|---|---|---|
| `graph` | `write:social-graph` | `user-only` | `w:action` | 3 | idempotent | `tweet.read`, `users.read`, `follows.write` |

| Field | Type | Required | Description |
|---|---|:--:|---|
| `user` | string (non-empty) | ✅ | Target user: numeric id, handle, @handle, or profile URL (not "me"). |
| `action` | `follow` \| `unfollow` | ✅ | Whether to follow or unfollow the user. |

### `x_mute_set`

**Mute / unmute a user** — Mute or unmute a user as the authenticated user.

| Package | Cell | Availability | Cost | Phase | MCP hints | OAuth scopes |
|---|---|---|---|---|---|---|
| `graph` | `write:social-graph` | `user-only` | `w:action` | 3 | idempotent | `tweet.read`, `users.read`, `mute.write` |

| Field | Type | Required | Description |
|---|---|:--:|---|
| `user` | string (non-empty) | ✅ | Target user: numeric id, handle, @handle, or profile URL (not "me"). |
| `action` | `mute` \| `unmute` | ✅ | Whether to mute or unmute the user. |

### `x_block_set`

**Block / unblock a user** — Block or unblock a user as the authenticated user.

| Package | Cell | Availability | Cost | Phase | MCP hints | OAuth scopes |
|---|---|---|---|---|---|---|
| `graph` | `destructive:social-graph` | `user-only` | `w:action` | 3 | destructive, non-idempotent | `tweet.read`, `users.read`, `block.write` |

| Field | Type | Required | Description |
|---|---|:--:|---|
| `user` | string (non-empty) | ✅ | Target user: numeric id, handle, @handle, or profile URL (not "me"). |
| `action` | `block` \| `unblock` | ✅ | Whether to block or unblock the user. |

### `x_followers_list`

**List a user's followers** — List the accounts following an X (Twitter) user.

| Package | Cell | Availability | Cost | Phase | MCP hints | OAuth scopes |
|---|---|---|---|---|---|---|
| `graph` | `read:social-graph` | `app+user` | `r:follows` | 3 | read-only | `tweet.read`, `users.read`, `follows.read` |

| Field | Type | Required | Description |
|---|---|:--:|---|
| `user` | string (non-empty) | ✅ | User to read: numeric id, handle, @handle, profile URL, or "me". |
| `max_results` | integer |  | Results per page (1-1000); out-of-range values are clamped into the window. |
| `page_token` | string |  | Opaque pagination cursor returned as next_token by a previous call. |
| `raw` | boolean |  | Return the exact API JSON (capped at 25 items) instead of the compact page. |

### `x_following_list`

**List accounts a user follows** — List the accounts an X (Twitter) user follows.

| Package | Cell | Availability | Cost | Phase | MCP hints | OAuth scopes |
|---|---|---|---|---|---|---|
| `graph` | `read:social-graph` | `app+user` | `r:follows` | 3 | read-only | `tweet.read`, `users.read`, `follows.read` |

| Field | Type | Required | Description |
|---|---|:--:|---|
| `user` | string (non-empty) | ✅ | User to read: numeric id, handle, @handle, profile URL, or "me". |
| `max_results` | integer |  | Results per page (1-1000); out-of-range values are clamped into the window. |
| `page_token` | string |  | Opaque pagination cursor returned as next_token by a previous call. |
| `raw` | boolean |  | Return the exact API JSON (capped at 25 items) instead of the compact page. |

### `x_user_search`

**Search user profiles** — Keyword search over X (Twitter) user profiles (names, handles, bios).

| Package | Cell | Availability | Cost | Phase | MCP hints | OAuth scopes |
|---|---|---|---|---|---|---|
| `graph` | `read:user` | `app+user` | `r:user` | 3 | read-only | `tweet.read`, `users.read` |

| Field | Type | Required | Description |
|---|---|:--:|---|
| `query` | string (non-empty) | ✅ | Keyword search over profiles (name, handle, bio). |
| `max_results` | integer |  | Results per page (1-1000); out-of-range values are clamped into the window. |
| `page_token` | string |  | Opaque pagination cursor returned as next_token by a previous call. |
| `raw` | boolean |  | Return the exact API JSON (capped at 25 items) instead of the compact page. |

## `lists`

### `x_list_create`

**Create a list** — Create a list owned by the authenticated user.

| Package | Cell | Availability | Cost | Phase | MCP hints | OAuth scopes |
|---|---|---|---|---|---|---|
| `lists` | `write:content` | `user-only` | `w:list` | 3 | — | `tweet.read`, `users.read`, `list.write` |

| Field | Type | Required | Description |
|---|---|:--:|---|
| `name` | string (1–25 chars) | ✅ | List name (1-25 characters). |
| `description` | string (max 100 chars) |  | List description (up to 100 characters). |
| `private` | boolean |  | Whether the list is private (visible only to its owner). |

### `x_list_update`

**Update a list** — Update the authenticated user's own list metadata — `name`, `description`, and/or `private`.

| Package | Cell | Availability | Cost | Phase | MCP hints | OAuth scopes |
|---|---|---|---|---|---|---|
| `lists` | `write:content` | `user-only` | `w:action` | 3 | — | `tweet.read`, `users.read`, `list.write` |

| Field | Type | Required | Description |
|---|---|:--:|---|
| `list_id` | string (non-empty) | ✅ | Target list: a numeric list id or a list URL (e.g. https://x.com/i/lists/123). |
| `name` | string (1–25 chars) |  | List name (1-25 characters). |
| `description` | string (max 100 chars) |  | List description (up to 100 characters). |
| `private` | boolean |  | Whether the list is private (visible only to its owner). |

### `x_list_delete`

**Delete a list** — Permanently delete the authenticated user's own list.

| Package | Cell | Availability | Cost | Phase | MCP hints | OAuth scopes |
|---|---|---|---|---|---|---|
| `lists` | `destructive:content` | `user-only` | `w:action` | 3 | destructive, non-idempotent | `tweet.read`, `users.read`, `list.write` |

| Field | Type | Required | Description |
|---|---|:--:|---|
| `list_id` | string (non-empty) | ✅ | Target list: a numeric list id or a list URL (e.g. https://x.com/i/lists/123). |

### `x_list_get`

**Get list metadata** — Read one list's metadata — name, description, privacy, member and follower counts, and owner handle.

| Package | Cell | Availability | Cost | Phase | MCP hints | OAuth scopes |
|---|---|---|---|---|---|---|
| `lists` | `read:content` | `app+user` | `r:list` | 3 | read-only | `tweet.read`, `users.read`, `list.read` |

| Field | Type | Required | Description |
|---|---|:--:|---|
| `list_id` | string (non-empty) | ✅ | Target list: a numeric list id or a list URL (e.g. https://x.com/i/lists/123). |
| `raw` | boolean |  | Return the exact API JSON (capped at 25 items) instead of the compact page. |

### `x_lists_owned`

**List owned lists** — The lists a user owns (defaults to the authenticated user).

| Package | Cell | Availability | Cost | Phase | MCP hints | OAuth scopes |
|---|---|---|---|---|---|---|
| `lists` | `read:content` | `app+user` | `owned` | 3 | read-only | `tweet.read`, `users.read`, `list.read` |

| Field | Type | Required | Description |
|---|---|:--:|---|
| `user` | string (non-empty) |  | Owner whose lists to read: numeric id, handle, @handle, profile URL, or "me" (default). |
| `max_results` | integer |  | Results per page (1-100); out-of-range values are clamped into the window. |
| `page_token` | string |  | Opaque pagination cursor returned as next_token by a previous call. |
| `raw` | boolean |  | Return the exact API JSON (capped at 25 items) instead of the compact page. |

### `x_list_member_set`

**Add / remove a list member** — Add a user to the authenticated user's own list or remove one — a single user per call.

| Package | Cell | Availability | Cost | Phase | MCP hints | OAuth scopes |
|---|---|---|---|---|---|---|
| `lists` | `write:content` | `user-only` | `w:action` | 3 | idempotent | `tweet.read`, `users.read`, `list.write` |

| Field | Type | Required | Description |
|---|---|:--:|---|
| `list_id` | string (non-empty) | ✅ | Target list: a numeric list id or a list URL (e.g. https://x.com/i/lists/123). |
| `user` | string (non-empty) | ✅ | User: numeric id, handle, @handle, profile URL, or "me". |
| `action` | `add` \| `remove` | ✅ | Whether to add or remove the member. |

### `x_list_members`

**List members of a list** — The members of a list.

| Package | Cell | Availability | Cost | Phase | MCP hints | OAuth scopes |
|---|---|---|---|---|---|---|
| `lists` | `read:content` | `app+user` | `r:user` | 3 | read-only | `tweet.read`, `users.read`, `list.read` |

| Field | Type | Required | Description |
|---|---|:--:|---|
| `list_id` | string (non-empty) | ✅ | Target list: a numeric list id or a list URL (e.g. https://x.com/i/lists/123). |
| `max_results` | integer |  | Results per page (1-100); out-of-range values are clamped into the window. |
| `page_token` | string |  | Opaque pagination cursor returned as next_token by a previous call. |
| `raw` | boolean |  | Return the exact API JSON (capped at 25 items) instead of the compact page. |

### `x_list_timeline`

**Read a list's timeline** — Posts from a list's timeline (recent posts by its members).

| Package | Cell | Availability | Cost | Phase | MCP hints | OAuth scopes |
|---|---|---|---|---|---|---|
| `lists` | `read:content` | `app+user` | `r:post` | 3 | read-only | `tweet.read`, `users.read`, `list.read` |

| Field | Type | Required | Description |
|---|---|:--:|---|
| `list_id` | string (non-empty) | ✅ | Target list: a numeric list id or a list URL (e.g. https://x.com/i/lists/123). |
| `max_results` | integer |  | Results per page (1-100); out-of-range values are clamped into the window. |
| `page_token` | string |  | Opaque pagination cursor returned as next_token by a previous call. |
| `raw` | boolean |  | Return the exact API JSON (capped at 25 items) instead of the compact page. |

### `x_list_follow_set`

**Follow / unfollow a list** — Follow a list as the authenticated user, or unfollow it.

| Package | Cell | Availability | Cost | Phase | MCP hints | OAuth scopes |
|---|---|---|---|---|---|---|
| `lists` | `write:engagement` | `user-only` | `w:action` | 3 | idempotent | `tweet.read`, `users.read`, `list.write` |

| Field | Type | Required | Description |
|---|---|:--:|---|
| `list_id` | string (non-empty) | ✅ | Target list: a numeric list id or a list URL (e.g. https://x.com/i/lists/123). |
| `action` | `follow` \| `unfollow` | ✅ | Whether to follow or unfollow the list. |

### `x_list_pin_set`

**Pin / unpin a list** — Pin a list in the authenticated user's list view, or unpin it.

| Package | Cell | Availability | Cost | Phase | MCP hints | OAuth scopes |
|---|---|---|---|---|---|---|
| `lists` | `write:engagement` | `user-only` | `w:action` | 3 | idempotent | `tweet.read`, `users.read`, `list.write` |

| Field | Type | Required | Description |
|---|---|:--:|---|
| `list_id` | string (non-empty) | ✅ | Target list: a numeric list id or a list URL (e.g. https://x.com/i/lists/123). |
| `action` | `pin` \| `unpin` | ✅ | Whether to pin or unpin the list. |

## `media`

### `x_media_upload`

**Upload media** — Upload a local image, GIF, or video via the chunked v2 flow and return a `media_id` to attach with `x_post_create`.

| Package | Cell | Availability | Cost | Phase | MCP hints | OAuth scopes |
|---|---|---|---|---|---|---|
| `media` | `write:content` | `user-only` | `w:action` | 3 | — | `media.write` |

| Field | Type | Required | Description |
|---|---|:--:|---|
| `path` | string (non-empty) | ✅ | Path of the media file to upload (image, GIF, or video). Must be inside the operator-configured media directory; relative paths resolve against it. |
| `media_category` | `tweet_image` \| `tweet_gif` \| `tweet_video` |  | Upload category for the composer. Defaults to the one implied by the file’s actual content (image -> tweet_image, GIF -> tweet_gif, video -> tweet_video). |
| `alt_text` | string (1–1000 chars) |  | Accessibility description attached to the media after upload (max 1000 chars). |

### `x_media_status`

**Check media processing status** — Check the async processing state of an uploaded media by `media_id`.

| Package | Cell | Availability | Cost | Phase | MCP hints | OAuth scopes |
|---|---|---|---|---|---|---|
| `media` | `read:content` | `user-only` | `local` | 3 | read-only | `media.write` |

| Field | Type | Required | Description |
|---|---|:--:|---|
| `media_id` | string (non-empty) | ✅ | The media id returned by x_media_upload whose processing state to check. |

## `dm`

### `x_dm_events_list`

**List DM events** — List all recent direct-message events across the authenticated X (Twitter) user's conversations, newest first.

| Package | Cell | Availability | Cost | Phase | MCP hints | OAuth scopes |
|---|---|---|---|---|---|---|
| `dm` | `read:dm` | `user-only` | `r:dm` | 3 | read-only | `dm.read`, `tweet.read`, `users.read` |

| Field | Type | Required | Description |
|---|---|:--:|---|
| `max_results` | integer |  | Results per page (1-100); out-of-range values are clamped into the window. |
| `page_token` | string |  | Opaque pagination cursor returned as next_token by a previous call. |
| `include_text` | boolean |  | Include sanitized message bodies and media (default false: only ids, timestamps, and participants are returned). |

### `x_dm_conversation_events_list`

**List DM events in a conversation** — List the direct-message events of one X (Twitter) DM conversation, newest first.

| Package | Cell | Availability | Cost | Phase | MCP hints | OAuth scopes |
|---|---|---|---|---|---|---|
| `dm` | `read:dm` | `user-only` | `r:dm` | 3 | read-only | `dm.read`, `tweet.read`, `users.read` |

| Field | Type | Required | Description |
|---|---|:--:|---|
| `conversation_id` | string (non-empty) | ✅ | DM conversation id: numeric, or two numeric ids joined by "-". |
| `max_results` | integer |  | Results per page (1-100); out-of-range values are clamped into the window. |
| `page_token` | string |  | Opaque pagination cursor returned as next_token by a previous call. |
| `include_text` | boolean |  | Include sanitized message bodies and media (default false: only ids, timestamps, and participants are returned). |

### `x_dm_participant_events_list`

**List DM events with a participant** — List the direct-message events of the 1:1 X (Twitter) DM conversation with one participant, newest first.

| Package | Cell | Availability | Cost | Phase | MCP hints | OAuth scopes |
|---|---|---|---|---|---|---|
| `dm` | `read:dm` | `user-only` | `r:dm` | 3 | read-only | `dm.read`, `tweet.read`, `users.read` |

| Field | Type | Required | Description |
|---|---|:--:|---|
| `participant` | string (non-empty) | ✅ | The other participant: numeric user id, handle, or @handle. |
| `max_results` | integer |  | Results per page (1-100); out-of-range values are clamped into the window. |
| `page_token` | string |  | Opaque pagination cursor returned as next_token by a previous call. |
| `include_text` | boolean |  | Include sanitized message bodies and media (default false: only ids, timestamps, and participants are returned). |

### `x_dm_send`

**Send a direct message** — Send an X (Twitter) direct message to exactly one target: an existing conversation (conversation_id) or a user (participant), creating the 1:1 conversation if needed.

| Package | Cell | Availability | Cost | Phase | MCP hints | OAuth scopes |
|---|---|---|---|---|---|---|
| `dm` | `write:dm` | `user-only` | `w:dm` | 3 | — | `dm.write`, `tweet.read`, `users.read` |

| Field | Type | Required | Description |
|---|---|:--:|---|
| `text` | string (1–10000 chars) | ✅ | Message text to send (1-10,000 characters). |
| `conversation_id` | string (non-empty) |  | Target conversation id (numeric, or two numeric ids joined by "-"). |
| `participant` | string (non-empty) |  | Target user for a 1:1 DM: numeric id, handle, or @handle. |

## `archive`

### `x_search_archive`

**Search the full archive** — Search the complete X (Twitter) archive back to 2006 using the full v2 query syntax (from:, to:, conversation_id:, boolean operators).

| Package | Cell | Availability | Cost | Phase | MCP hints | OAuth scopes |
|---|---|---|---|---|---|---|
| `archive` | `read:content` | `app+user` | `r:post` | 3 | read-only | `tweet.read`, `users.read` |

| Field | Type | Required | Description |
|---|---|:--:|---|
| `query` | string (non-empty) | ✅ | X (Twitter) v2 search query, e.g. "from:xdevelopers -is:retweet". |
| `max_results` | integer |  | Results per page (10-500); out-of-range values are clamped into the window. |
| `page_token` | string |  | Opaque pagination cursor returned as next_token by a previous call. |
| `start_time` | string |  | Oldest post timestamp to include (ISO-8601 UTC). |
| `end_time` | string |  | Newest post timestamp to include (ISO-8601 UTC). |
| `sort_order` | `recency` \| `relevancy` |  | Result ordering; defaults to recency. |
| `raw` | boolean |  | Return the exact API JSON (capped at 25 items) instead of the compact page. |

### `x_post_counts_archive`

**Count posts across the archive** — Return a volume histogram (post counts per time bucket) for an X (Twitter) v2 query over the complete archive back to 2006, at minute/hour/day granularity.

| Package | Cell | Availability | Cost | Phase | MCP hints | OAuth scopes |
|---|---|---|---|---|---|---|
| `archive` | `read:content` | `app+user` | `r:post` | 3 | read-only | `tweet.read` |

| Field | Type | Required | Description |
|---|---|:--:|---|
| `query` | string (non-empty) | ✅ | X (Twitter) v2 search query to count. |
| `granularity` | `minute` \| `hour` \| `day` |  | Histogram bucket size; defaults to hour. |
| `start_time` | string |  | Oldest bucket timestamp (ISO-8601 UTC). |
| `end_time` | string |  | Newest bucket timestamp (ISO-8601 UTC). |
| `page_token` | string |  | Opaque pagination cursor returned as next_token by a previous call. |
| `raw` | boolean |  | Return the exact API JSON instead of the compact histogram. |

## `usage`

### `x_usage_get`

**Usage and spend** — Report the post-read consumption of the current billing cycle against the monthly project cap (with an optional per-day and per-app breakdown), alongside the local credit-spend estimate for this session.

| Package | Cell | Availability | Cost | Phase | MCP hints | OAuth scopes |
|---|---|---|---|---|---|---|
| `usage` | `read:account` | `app+user` | `owned` | 3 | read-only | — |

| Field | Type | Required | Description |
|---|---|:--:|---|
| `days` | integer |  | Days of daily breakdown to return (1-90; the API defaults to 7). Out-of-range values are clamped into the window. |
| `raw` | boolean |  | Return the exact API JSON instead of the compact report. |
