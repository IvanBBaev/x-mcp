# Build & test artifacts (committed ignore; AI-harness files live in .git/info/exclude)
node_modules/
build/
.build-*/
coverage/
*.log

# Local environment / secrets
.env
.env.*
!.env.example

# OS noise
.DS_Store
